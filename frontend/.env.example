# Base URL of the backend API, reachable from the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# Hostname serving apartment media (used to allow next/image remote sources).
NEXT_PUBLIC_MEDIA_HOST=prod-images.cooingestate.com
